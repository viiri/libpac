gus-ramping.txt contain excerpts from GUS-HPC.TXT, the Gravis UltraSound
HelpPC-HelpKit version 0.85, regarding Gravis UltraSound hardware volume
ramping.

magic tells file(1) about SBStudio PAC music and SOU sound files.

pacform.patch corrects mistakes in the original pacform.txt format description
from SBStudio 3.60.

`make' creates libpac.info, `make html' the HTML manual, and `make all'
creates both.  This requires that makeinfo is installed.
